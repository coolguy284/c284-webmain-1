```
format for nodesrv_main/website_data.txt:
  # repeated paths' numbers are added together, "/*" at end of path to include entire folder
  <path> : int (flags) (
      bits:
        1 - immutable
    ),
  "#" : comment,
```
